/**
 * @description 4 sub-configurations, cli configuration | general configuration | theme configuration | network configuration export
 */
import { cliConfig } from '/@/config/cli.config'
import { netConfig } from '/@/config/net.config'
import { settingConfig } from '/@/config/setting.config'
import { themeConfig } from '/@/config/theme.config'

const config: { [key: string]: any } = {
  ...cliConfig,
  ...netConfig,
  ...settingConfig,
  ...themeConfig,
}

export default config

export const {
  abbreviation,
  assetsDir,
  authentication,
  base,
  chunkSizeWarningLimit,
  color,
  colorWeakness,
  columnStyle,
  compress,
  contentType,
  copyright,
  cssCodeSplit,
  debounce,
  defaultOpeneds,
  errorLog,
  exclude,
  fixedHeader,
  foldSidebar,
  https,
  i18n,
  include,
  isFollow,
  isHashRouterMode,
  keepAliveMaxNum,
  layout,
  loadingText,
  localEnabled,
  loginInterception,
  loginRSA,
  logo,
  menuWidth,
  messageDuration,
  messageName,
  minify,
  noDebugger,
  open,
  openFirstMenu,
  outDir,
  outputHash,
  pageTransition,
  persistenceTab,
  port,
  prodEnabled,
  pwa,
  pwaDev,
  radius,
  recordRoute,
  report,
  reportCompressedSize,
  rolesControl,
  routesWhiteList,
  showColorPicker,
  showDark,
  showFooter,
  showFullScreen,
  showLanguage,
  showLock,
  showNotice,
  showProgressBar,
  showRefresh,
  showSearch,
  showTabs,
  showTabsIcon,
  showTheme,
  showThemeSetting,
  statusName,
  storage,
  successCode,
  supportVisit,
  tabsBarStyle,
  themeName,
  timeout,
  title,
  titleReverse,
  titleSeparator,
  tokenName,
  tokenTableName,
  uniqueOpened,
} = config
